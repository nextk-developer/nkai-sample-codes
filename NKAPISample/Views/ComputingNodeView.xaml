<UserControl x:Class="NKAPISample.Views.ComputingNodeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:dxlc="http://schemas.devexpress.com/winfx/2008/xaml/layoutcontrol"
             xmlns:ui="clr-namespace:NKAPISample.Controls"
             xmlns:ViewModels="clr-namespace:NKAPISample.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
            <RowDefinition />
        </Grid.RowDefinitions>
        <ui:LabelledTextBox Header="Host IP" Text="{Binding HostIP, Mode=TwoWay}"/>
        <!--<TextBox Text="{Binding HostIP}"/>-->
        <ui:LabelledTextBox Header="Host Port" Text="{Binding HostPort}" Grid.Row="1"/>
        <ui:LabelledTextBox Header="Node Name" Text="{Binding NodeName}" Grid.Row="2"/>
        
    </Grid>
</UserControl>
