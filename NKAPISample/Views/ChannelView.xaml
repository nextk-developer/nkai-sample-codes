<UserControl x:Class="NKAPISample.Views.ChannelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ui="clr-namespace:NKAPISample.Controls"
             xmlns:channelAPI="clr-namespace:NKAPIService.API.Channel.Models;assembly=NKAPIService"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d"
             xmlns:vm="clr-namespace:NKAPISample.ViewModels"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Margin="0,5,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition />
            <ColumnDefinition Width="5"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <ui:LabelledTextBox Header="Channel URL" Text="{Binding ChannelURL}" Grid.ColumnSpan="6"/>
        <Button Content="Create" Command="{Binding CreateCommand}" Grid.Column="1" Grid.Row="1"/>
        <Button Content="Get" Command="{Binding GetCommand}" Grid.Column="3" Grid.Row="1"/>
        <Button Content="Remove" Command="{Binding RemoveCommand}" Grid.Column="5" Grid.Row="1"/>
    </Grid>
</UserControl>
