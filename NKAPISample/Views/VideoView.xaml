<UserControl x:Class="NKAPISample.Views.VideoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:fl="clr-namespace:FlyleafLib.Controls.WPF;assembly=FlyleafLib"
             xmlns:vm="clr-namespace:NKAPISample.ViewModels"
             xmlns:uc="clr-namespace:NKAPISample.Controls" d:DataContext="{d:DesignInstance Type=vm:VideoViewModel}"
             Loaded="VideoView_Loaded"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="boolToVisibilityConverter"/>
    </UserControl.Resources>
    <Grid>
        <fl:FlyleafHost Player="{Binding Player}" >
            <Grid>
                <Grid Name="drawCanvas"/>
                <Grid Name="drawRange" Visibility="{Binding IsDrawingMode, Converter={StaticResource boolToVisibilityConverter}}" PreviewMouseDown="drawRange_PreviewMouseDown" PreviewMouseUp="drawRange_PreviewMouseUp"/>
            </Grid>
        </fl:FlyleafHost>
        
    </Grid>
</UserControl>
