<UserControl 
    x:Class="NKAPISample.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:conv="clr-namespace:NKAPISample.Views.Converter"
    xmlns:Views="clr-namespace:NKAPISample.Views"
    xmlns:ViewModels="clr-namespace:NKAPISample.ViewModels"
    xmlns:dxdo="http://schemas.devexpress.com/winfx/2008/xaml/docking" 
    xmlns:dxlc="http://schemas.devexpress.com/winfx/2008/xaml/layoutcontrol"
    mc:Ignorable="d" d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.DataContext>
        <ViewModels:MainViewModel/>
    </UserControl.DataContext>
    <UserControl.Resources>
        <Style TargetType="{x:Type dxdo:LayoutPanel}">
            <Setter Property="ShowPinButton" Value="False"/>
            <Setter Property="ShowCloseButton" Value="False"/>
            <Setter Property="Padding" Value="8,3,8,8"/>
        </Style>
        <Style TargetType="{x:Type Button}">
            <Setter Property="Height" Value="27"/>
        </Style>
        <conv:BoolToEnumConverter x:Key="BoolToEnumConverter"/>
    </UserControl.Resources>
    <Grid>
        <dxdo:DockLayoutManager>
            <dxdo:LayoutGroup Orientation="Vertical">
                <dxdo:LayoutGroup Orientation="Horizontal">
                    <dxdo:LayoutGroup Orientation="Vertical" Caption="Setting" ShowCaption="True">
                        <dxdo:LayoutPanel Caption="Setting">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition />
                                    <RowDefinition Height="35"/>
                                </Grid.RowDefinitions>

                                <dxlc:LayoutGroup View="Tabs" SelectedTabIndex="{Binding SelectedObject, Mode=OneWayToSource}">
                                    <dxlc:LayoutGroup.Resources>
                                        <Style TargetType="{x:Type dxlc:LayoutGroup}">
                                            <Setter Property="ItemLabelsAlignment" Value="Local"/>
                                            <Setter Property="Orientation" Value="Vertical"/>
                                        </Style>
                                    </dxlc:LayoutGroup.Resources>

                                    <dxlc:LayoutGroup Header="Computing Node" Orientation="Vertical" ItemLabelsAlignment="Local">
                                        <UserControl Content="{Binding NodeView}" />
                                    </dxlc:LayoutGroup>
                                    <dxlc:LayoutGroup Header="Channel">
                                        <Views:ChannelView />
                                    </dxlc:LayoutGroup>
                                    <dxlc:LayoutGroup Header="ROI">
                                        <Views:ROIView />
                                    </dxlc:LayoutGroup>
                                </dxlc:LayoutGroup>
                                <Grid Grid.Row="1">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition/>
                                        <ColumnDefinition/>
                                        <ColumnDefinition/>
                                    </Grid.ColumnDefinitions>
                                    <Button Content="Create" Command="{Binding CreateCommand}"/>
                                    <Button Content="Get" Command="{Binding GetCommand}" Grid.Column="1"/>
                                    <Button Content="Remove" Command="{Binding RemoveCommand}"  Grid.Column="2"/>
                                </Grid>
                            </Grid>
                        </dxdo:LayoutPanel>
                    </dxdo:LayoutGroup>
                    <dxdo:LayoutGroup Orientation="Vertical">
                        <dxdo:LayoutPanel Caption="Post" ItemHeight="70" CaptionAlignMode="Custom">
                            <TextBox Text="{Binding PostURI}"/>
                        </dxdo:LayoutPanel>
                        <dxdo:LayoutPanel Caption="Request API">
                            <TextBox Text="{Binding RequestResult}"/>
                        </dxdo:LayoutPanel>
                        <dxdo:LayoutPanel Caption="Response API / Log">
                            <TextBox Text="{Binding ResponseResult}"/>
                        </dxdo:LayoutPanel>
                    </dxdo:LayoutGroup>
                    
                </dxdo:LayoutGroup>
                <dxdo:LayoutGroup Orientation="Horizontal">
                    <dxdo:LayoutPanel Caption="Streaming">
                        <Canvas Background="Black"/>
                    </dxdo:LayoutPanel>
                    <dxdo:LayoutPanel Caption="Metadata Log">
                        <RichTextBox />
                    </dxdo:LayoutPanel>
                </dxdo:LayoutGroup>
            </dxdo:LayoutGroup>
        </dxdo:DockLayoutManager>
    </Grid>
</UserControl>
