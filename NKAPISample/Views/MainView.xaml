<UserControl 
    x:Class="NKAPISample.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:conv="clr-namespace:NKAPISample.Views.Converter"
    xmlns:Views="clr-namespace:NKAPISample.Views"
    xmlns:ViewModels="clr-namespace:NKAPISample.ViewModels"
    xmlns:ui ="clr-namespace:NKAPISample.Controls"
    mc:Ignorable="d" d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.DataContext>
        <ViewModels:MainViewModel/>
    </UserControl.DataContext>
    <UserControl.Resources>
        <Style TargetType="{x:Type Button}">
            <Setter Property="Height" Value="25"/>
        </Style>
        <conv:BoolToEnumConverter x:Key="BoolToEnumConverter"/>
        <Style TargetType="Border">
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="LightGray"/>
            <Setter Property="Margin" Value="3"/>
            <Setter Property="Padding" Value="5"/>
        </Style>
        <Style TargetType="GroupBox">
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <!--#region Row 0 Left -->
        <Border>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Label Content="Settings" Foreground="DodgerBlue" FontSize="14" FontWeight="Light"/>
                <GroupBox Grid.Row="1" Header="Computing Node">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition />
                            <RowDefinition Height="35"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        
                        <UserControl Content="{Binding NodeView}" Grid.ColumnSpan="4"/>
                        <Button Content="Create" Command="{Binding CreateCommand}" Grid.Row="1" Grid.Column="1" Margin="0,0,1.5,0"/>
                        <Button Content="Get" Command="{Binding GetCommand}" Grid.Column="2" Grid.Row="1" Margin="1.5,0,1.5,0"/>
                        <Button Content="Remove" Command="{Binding RemoveCommand}"  Grid.Column="3" Grid.Row="1" Margin="1.5,0,0,0"/>
                    </Grid>
                </GroupBox>
                <GroupBox Grid.Row="2" Height="40">
                    <Grid Margin="0,5,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <Label Content="Channel" VerticalAlignment="Center"/>
                        <Button Content="Create" Command="{Binding CreateCommand}" Grid.Row="1" Grid.Column="1" Margin="0,0,1.5,0" Tag="Channel"/>
                        <Button Content="Get" Command="{Binding GetCommand}" Grid.Column="2" Grid.Row="1" Margin="1.5,0,1.5,0"/>
                        <Button Content="Remove" Command="{Binding RemoveCommand}"  Grid.Column="3" Grid.Row="1" Margin="1.5,0,0,0"/>
                    </Grid>
                </GroupBox>
                <GroupBox Grid.Row="3" Height="40">
                    <Grid Margin="0,5,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <Label Content="Schedule" VerticalAlignment="Center"/>
                        <Button Content="Create" Command="{Binding CreateCommand}" Grid.Row="1" Grid.Column="1" Margin="0,0,1.5,0"/>
                        <Button Content="Get" Command="{Binding GetCommand}" Grid.Column="2" Grid.Row="1" Margin="1.5,0,1.5,0"/>
                        <Button Content="Remove" Command="{Binding RemoveCommand}"  Grid.Column="3" Grid.Row="1" Margin="1.5,0,0,0"/>
                    </Grid>
                </GroupBox>
                <GroupBox Grid.Row="4" Height="40">
                    <Grid Margin="0,5,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <Label Content="RoI" VerticalAlignment="Center"/>
                        <Button Content="Create" Command="{Binding CreateCommand}" Grid.Row="1" Grid.Column="1" Margin="0,0,1.5,0"/>
                        <Button Content="Get" Command="{Binding GetCommand}" Grid.Column="2" Grid.Row="1" Margin="1.5,0,1.5,0"/>
                        <Button Content="Remove" Command="{Binding RemoveCommand}"  Grid.Column="3" Grid.Row="1" Margin="1.5,0,0,0"/>
                    </Grid>
                </GroupBox>
                <GroupBox Grid.Row="5" Height="40">
                    <Grid Margin="0,5,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>
                        <Label Content="VA" VerticalAlignment="Center"/>
                        <Button Content="Create" Command="{Binding CreateCommand}" Grid.Row="1" Grid.Column="1" Margin="0,0,1.5,0"/>
                        <Button Content="Get" Command="{Binding GetCommand}" Grid.Column="2" Grid.Row="1" Margin="1.5,0,1.5,0"/>
                        <Button Content="Remove" Command="{Binding RemoveCommand}"  Grid.Column="3" Grid.Row="1" Margin="1.5,0,0,0"/>
                    </Grid>
                </GroupBox>
            </Grid>
        </Border>
        <!--#endregion-->
        <!--#region Row 0 Right -->
        <Grid Grid.Column="1">
            <Grid.Resources>
                <Style TargetType="{x:Type Label}">
                    <Setter Property="FontSize" Value="14"/>
                    <Setter Property="Foreground" Value="DodgerBlue"/>
                    <Setter Property="FontWeight" Value="Light"/>
                </Style>
            </Grid.Resources>
            <Grid.RowDefinitions>
                <RowDefinition Height="70" />
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>
            <Border>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <Label Content="Post URL"/>
                    <TextBox Text="{Binding PostURL}" Grid.Row="1" VerticalAlignment="Stretch"/>
                </Grid>
            </Border>
            <Border Grid.Row="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <Label Content="Request API"/>
                    <TextBox Text="{Binding RequestResult}" AcceptsReturn="True" VerticalAlignment="Stretch" Grid.Row="1"/>
                </Grid>
            </Border>
            <Border BorderThickness="1" BorderBrush="LightGray" Grid.Row="2">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="125"/>
                    </Grid.RowDefinitions >
                    <Label Content="Response API / Log"/>
                    <TextBox Text="{Binding ResponseResult}" ScrollViewer.VerticalScrollBarVisibility="Visible" VerticalAlignment="Stretch" Grid.Row="1"/>
                </Grid>
            </Border>
        </Grid>
        <!--#endregion-->

        <Border BorderThickness="1" BorderBrush="LightGray" Grid.Row="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <Label Content="Streaming"/>
                <Canvas Background="Black" Grid.Row="1" VerticalAlignment="Stretch"/>
            </Grid>
        </Border>
        <Border BorderThickness="1" BorderBrush="LightGray" Grid.Row="1" Grid.Column="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <Label Content="Metadata Log"/>
                <TextBox Grid.Row="1" VerticalAlignment="Stretch"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
