<UserControl x:Class="NKAPISample.Views.VAView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ViewModels="clr-namespace:NKAPISample.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Margin="0,5,0,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="5"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Label Content="VA" VerticalAlignment="Center" Name="lbGroupHeader5"/>
        <Button Content="Start" Command="{Binding StartCommand}" Grid.Row="1" Grid.Column="1"/>
        <Button Content="Reset" Command="{Binding ResetCommand}" Grid.Column="3" Grid.Row="1" />
        <Button Content="Stop" Command="{Binding StopCommand}" Grid.Column="5" Grid.Row="1" />
    </Grid>
</UserControl>
